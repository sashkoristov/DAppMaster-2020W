function main(params) {
	return { 
	    // return inputs
        folderVideos: params.folderVideos,

	    // return outputs
	    numberVideos: 3,
	    videos: ["1","2","3"],
        folderFrames: ["1", "2", "3"],
        folderVideos: "FolderVideos"
	};
}
